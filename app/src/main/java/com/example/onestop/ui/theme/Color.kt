package com.example.onestop.ui.theme

import androidx.compose.ui.graphics.Color

// ── Legacy theme colors ───────────────────────────────────────────────────────
val darkMyBackgroundColor  = Color(0xFF272b30)
val Purple80               = Color(0xFFe1b79e)
val LightBlue              = Color(0xFF364252)
val PurpleGrey80           = Color(0xFF5a333f)
val Pink80                 = Color(0xFFdde5d0)

val lightMyBackgroundColor = Color(0xFFE1B79E)
val Purple40               = Color(0xFFCE949E)
val PurpleGrey40           = Color(0xFF625b71)
val Pink40                 = Color(0xFF7D5260)

// ── OneStop Design System ─────────────────────────────────────────────────────

// Backgrounds
val BgColor    = Color(0xFF1C2130)   // screen background
val CardColor  = Color(0xFF253040)   // card / surface
val ItemColor  = Color(0xFF2E3A4E)   // list item
val FieldColor = Color(0xFF1E2A3A)   // text field background

// Text
val TextPrimary  = Color(0xFFE0E6F0)  // body text
val TextSub      = Color(0xFF8A9BB0)  // secondary / caption
val HeadingColor = Color(0xFFDDE5D0)  // section headings
val TimerText    = Color(0xFFE8D9B5)  // warm cream — timer / accent text
val BodyText     = Color(0xFFE0E6F0)  // alias for TextPrimary
val SubText      = Color(0xFF8A9BB0)  // alias for TextSub

// Divider & stroke
val DividerColor = Color(0xFF3A4A5A)
val StrokeColor  = Color(0xFF3A4A5A)  // semantic alias

// Buttons
val ButtonColor = Color(0xFFD4A98A)   // primary CTA (peach)
val ButtonTxt   = Color(0xFF3A2A1A)

// Ring / Progress
val RingProgress = Color(0xFFB0C4DE)  // progress ring fill
val RingTrack    = Color(0xFF2E3A4E)  // progress ring track
val PlayColor    = Color(0xFFB0C4DE)  // play icon accent

// Daily Goals — progress bar gradient
val ProgressStart = Color(0xFFE05C7A)  // gradient start (pink-red)
val ProgressEnd   = Color(0xFFFF8FA3)  // gradient end (light pink)
val ProgressTrack = Color(0xFF3A4055)  // progress bar background

// Daily Goals — checkbox
val CheckboxColor = Color(0xFF8A8FA8)  // unchecked border
val CheckedColor  = Color(0xFF6C72CB)  // checked fill (indigo)

// Error
val ErrorColor = Color(0xFFE07A7A)


val GridBgColor   = Color(0xFF1E2538)
val GridItemColor = Color(0xFF273045)